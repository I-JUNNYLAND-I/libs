jdk:
  - openjdk17
before_install:
  - echo "Running a custom install command"
   - sdk install java 17.0.1-open
   - sdk use java 17.0.1-open
install:
   - echo "Running a custom install command"
   - ./changer/gradlew clean build
